class ClearSupplierExcelImportUseCase {
  const ClearSupplierExcelImportUseCase();

  void call() {
    // This use case intentionally has no persistence to clear for v1.
    // It exists to keep the Excel Import flow consistent with clean architecture.
  }
}
